#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main() {
    char buf[16];
    char magic[10];
    printf("Test binary\n");
//    if (strncmp(buf, "UTCN", 4) == 0)
//    fgets(magic, 10, stdin);

//    if(strcmp(magic, "a\n") == 0){
        gets(buf);
//    }

    return 0;
}